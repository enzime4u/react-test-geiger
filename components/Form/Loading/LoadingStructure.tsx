import React from "react";

import { LoadingContainer } from "./LoadingStyles";

const LoadingStructure: React.FC = () => {
  return (
    <LoadingContainer>
      <p>Loading films...</p>
    </LoadingContainer>
  );
};

export default LoadingStructure;
